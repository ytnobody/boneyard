name = "json"
author = "ytnobody"
description = "json parser / builder"
license = "MIT"
keywords = ["json", "data"]
entry = "mod.ca"

# Dependencies - only external modules need to be listed here
# Core modules (core.*) are part of stdlib and don't need to be declared
#
# [dependencies]
# "author/module" = "^1.0.0"

source_url = "https://github.com/ytnobody/calcium-json/refs/heads"
